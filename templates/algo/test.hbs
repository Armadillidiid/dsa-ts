import { describe, it } from 'node:test'; import assert from 'node:assert';
import {
{{camelCase name}}
} from './{{name}}.practice.js'; describe('{{camelCase name}}', () => {
it('should work with basic input', () => { // TODO: Add test case const result =
{{camelCase name}}(/* input */); assert.strictEqual(result, /* expected */); });
// TODO: Add more test cases });